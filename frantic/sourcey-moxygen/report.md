# Sourcey Docs Proof: moxygen

## Delivery

- Bounty: #46, Publish Sourcey docs for a second ecosystem.
- Public URL: https://0state.com/moxygen/docs
- Target repo: https://github.com/sourcey/moxygen
- Pinned commit: `2f14cd8354247e27553ee0d8f4139d5095234e16`
- License: MIT.
- Ecosystem: TypeScript / Node.js npm CLI and documentation tooling.
- Receipt: `runx:receipt:sha256:607fd09d97ac383abc2e24680bc3c492cc37f31784119e2213bd6286647a3983`

## Why This Fits

- The target is a maintained npm package and TypeScript library/CLI, not a Go or C++ target.
- The docs are live on `0state.com/moxygen/docs`, a project/maintainer-owned durable site linked from the moxygen project page.
- The site is generated by Sourcey from `docs/sourcey.config.ts`, using `defineConfig()`, markdown navigation groups, repo/edit links, and a static build command.
- The public site has generated navigation and a Sourcey build marker.
- The repository and docs are pinned to commit `2f14cd8354247e27553ee0d8f4139d5095234e16`, with MIT license evidence in `package.json` and `LICENCE`.
- The validation run used `runx-cli 0.6.15`, above the required 0.6.6 floor.

## Coverage

- Install and quickstart flow.
- Doxygen XML input expectations.
- `moxygen` CLI usage.
- `--groups`, `--classes`, and `--pages` output modes.
- Output filename patterns using `%s`.
- Anchor and HTML-anchor behavior.
- Language selection.
- Custom Handlebars templates.
- Source-root/source mapping.
- Programmatic `run()` usage.
- Programmatic `generate()` usage.
- `loadIndex` parser export.
- Default filters.
- Grouping modes.
- Example bicycle, mountain bike, and racing bike modules.

## Validation

- Ran `npx -y @runxhq/cli@0.6.15 --version` and got `runx-cli 0.6.15`.
- Ran a local runx validation skill against six public docs URLs.
- Checked `https://0state.com/moxygen/docs`.
- Checked `https://0state.com/moxygen/docs/quickstart`.
- Checked `https://0state.com/moxygen/docs/cli-reference`.
- Checked `https://0state.com/moxygen/docs/programmatic-api`.
- Checked `https://0state.com/moxygen/docs/custom-templates`.
- Checked `https://0state.com/moxygen/docs/grouping-modes`.
- Every checked URL returned HTTP 200 and contained Sourcey/project navigation markers.

## Notes

- The live site is guide/config/API-usage oriented rather than a full TypeScript symbol reference.
- The value is still reviewable: the docs surface gives moxygen users install, CLI, API, template, grouping, and Doxygen authoring guidance in one durable Sourcey build.
- No credentials, payout data, or private GoFrantic tokens are included in this artifact set.
